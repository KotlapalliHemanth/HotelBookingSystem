import React from 'react';

const Profile = () => {
    return (
        //needs to make a profile page with all the information of the user and bookings list of user
        <div className='profile box' id='profile'>
            Profile box
        </div>
    );
}

export default Profile;
